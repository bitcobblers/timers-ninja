import { Canvas, Meta } from '@storybook/blocks';

import * as TimerDigitsStories from './timer-digits.stories';

<Meta of={TimerDigitsStories} />

# Timer Display

The goal of timer display is to display:

- the time left/elapsed
- the direction/type of the count  (up / down / date)
- Labels / Rounds information

A typical timer looks like this:

{/* <Canvas of={TimerDigitsStories.Primary} />

An empty timer with no values looks like this:

<Canvas of={TimerDigitsStories.Empty} />

Negative values???

<Canvas of={TimerDigitsStories.Negative} /> */}